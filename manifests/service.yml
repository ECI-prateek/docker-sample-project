apiVersion: v1
kind: Service
metadata:
    name: eciprateekdockersampleproject
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: eciprateekdockersampleproject